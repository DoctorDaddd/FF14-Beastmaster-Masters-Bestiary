# FF14 本地魔兽图鉴

收录 50 种魔兽、49 张对应图片和 31 张离线地图，资料核对日期为 2026-09-09。网页运行无需联网、安装软件或启动服务器。

## 打开与记录

将整个 `ff14-bestiary` 文件夹放在固定位置，双击带金色兽首图标的 `启动魔兽图鉴.lnk`，即可用默认浏览器打开图鉴；也可以用 Chrome 或 Edge 打开 `index.html`。JavaScript、样式和 `assets` 图片目录需要与网页一起保留。

- 勾选卡片底部的“已捕获”记录进度，再次点击取消；进度自动保存在当前浏览器中。
- 点击图片或名称查看全部位置、地图红圈与获取说明。
- 名称搜索、栖息地区和捕获状态可组合筛选。
- 点击“隐藏已捕获”只显示未捕获条目，再次点击恢复显示；按钮与左侧状态筛选同步。
- 地区内全部魔兽捕获完成后，下拉栏名称后显示 ✅；取消其中任何一只会自动去勾。统计不受当前筛选影响，多坐标的同一魔兽只计算一次。

## 启动图标

金色兽首徽章同时用于启动快捷方式、浏览器标签页和网页标题左侧。图标文件为 `assets/bestiary-icon.ico`，含 16 至 256 像素的 7 种尺寸；另附可缩放 SVG 和高清 PNG。

解压到新位置或移动文件夹后，双击 `重新创建启动图标.vbs`，即可在当前文件夹重新生成正确指向网页和图标的快捷方式。若要放到桌面，可复制生成的快捷方式。首次更新网页图标后刷新页面即可。

## 排序

| 模式 | 比较顺序 |
| --- | --- |
| 编号升序 | 编号 |
| 等级升序 | 最低等级升序 → 地区 → 编号 |
| 等级降序 | 最低等级降序 → 地区 → 编号 |
| 位置／地区分类 | 地区 → 最低等级升序 → 编号 |

地区按中文名称排列。等级区间取最低等级，未知等级放在同一比较层级末尾。卡片默认显示首个位置；选择地区后显示该地区的首个位置。详情保留所有位置。

## 地图红圈

地图来自 Wiki 的地图组件，已离线保存。虚线红圈为 Wiki 提供的活动范围，实线小圈仅指示坐标点。多处位置同时显示，并与坐标列表编号对应。地图和红圈随窗口大小同步缩放。

副本可切换楼层。库西初始自带，无需地图；副本条目缺少具体捕获坐标，因此显示对应地图及未标注说明，不推测捕获位置。副本楼层不代表该魔兽已确认所在楼层。地图、坐标规则与瓦片来源见 `地图来源.json`。

40 号妖魂为 7 级，名称、等级及位置由用户补充：中拉诺西亚地下（X: 20.4，Y: 18.9），入口为（X: 20.4，Y: 20.3）。地图用红圈标注栖息位置，用金色菱形标注入口；入口不计作另一个栖息位置。

## 自动保存

沿用原来的浏览器存储位置，已有捕获记录仍可读取。每次勾选后，左下角显示保存状态。保存失败时会明确提示：当前操作仅保留在本页面中，关闭或刷新后可能丢失。

请在固定文件位置、同一浏览器的普通窗口使用。移动或改名网页、更换浏览器或清理浏览器数据后，原进度可能无法读取。

## 资料与验证

资料来源：[灰机 Wiki 魔兽图鉴](https://ff14.huijiwiki.com/wiki/魔兽图鉴)。使用 Scrapling 取得完整正文并逐行核对 1–50 号条目，图片来自对应资料页。详细来源见 `资料来源.json`。

来源缺项：库西暂缺图片，保留缺失提示。40 号妖魂的等级已根据用户提供的信息补为 7 级。

当前版本通过旧存档兼容、排序、存储失败提示等 5 项核心与 DOM 检查，以及 17 项真实 Chrome 检查，覆盖全部 50 个详情、31 张离线地图、41 处栖息位置红圈、楼层切换、地区绿勾、隐藏按钮、自动保存恢复和窄屏布局。测试使用独立配置，不改动实际使用中的捕获记录。

本工具为非官方玩家工具。FINAL FANTASY XIV 及游戏相关素材归 SQUARE ENIX 所有。
